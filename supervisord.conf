[supervisord]
nodaemon=true

[program:streamlit]
command=python entrypoint.py
directory=/app

[program:whatsapp]
command=uvicorn whatsapp_webhook.webhook:app --host 0.0.0.0 --port 8000
directory=/app
